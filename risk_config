{
  "version": "Mk-RiskConfig R10",
  "note": "日内 PnL / 回撤 / 最小权益 的风控闸门配置，单位均为百分比（%），绝对金额用 min_equity。",
  "thresholds": {
    "max_dd_pct": 12.0,
    "day_loss_pct": -4.0,
    "day_dd_pct": 6.0,
    "min_equity_pct_of_init": 0.5
    /*
      可选：如果你想直接写绝对金额，也可以加：
      "min_equity": 80000
      有这个字段时会优先用绝对值，其次才按比例算。
    */
  }
}
